# Normalize line endings to LF in the repo for cross-platform consistency.
* text=auto eol=lf

# Binary assets — never touch.
*.png binary
*.ico binary
*.icns binary
*.woff2 binary
*.woff binary
*.ttf binary
